{
  "name": "planazo-backend",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "db:generate": "pnpm --filter @planazo/api db:generate",
    "db:migrate": "pnpm --filter @planazo/api db:migrate",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "sync-shared": "bash scripts/sync-shared.sh"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  }
}
